# pg13::lsTables(conn = conn,
#                schema = "hemonc_extension")

# conn <- chariot::connect_athena()
# hemOncExt::createHemOncExtSchema(conn = conn)
# hemOncExt::ddlHemOncExtSchema(conn = conn)
# hemOncExt::migrateConcept(conn = conn,
#                           source_schema = "public")
# hemOncExt::migrateConceptAncestor(conn = conn,
#                                   source_schema = "public")
# migrateConceptAncestor(conn = conn,
#                        source_schema = "public")

# pg13::send(conn = conn,
#            sql_statement = "DELETE FROM hemonc_extension.concept_ancestor")



